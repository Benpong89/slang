import { connect } from 'react-redux';
import AddMessage from './add_message.jsx';


const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({
});

export default connect(null, mapDispatchToProps)(AddMessage);
